{
  "name": "magstock-v2",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "build": "npm --prefix apps/api run build && npm --prefix apps/web run build",
    "test": "npm --prefix apps/api test"
  }
}
